# 案例：將 Ultralytics YOLO 安全引入

本案例記錄一次完整的開源安全引入過程，依 [`開源安全檢查流程.md`](開源安全檢查流程.md) 的四關卡逐項執行。本文以敘事方式呈現，原始證據（執行紀錄、輸出截圖、決策依據）保留於 [`doc/`](doc/) 下三份報告。

讀者可同時開啟本文與 [`webcam.py`](webcam.py)，於每節結尾依指示動手執行對應步驟。預估完成時間：約 90 分鐘（含等待 PyTorch 下載）。

---

## 第一節：案例緣起

選擇 **Ultralytics YOLO** 作為案例，理由有三：

1. **代表性**：YOLO 是 2025 年代物件偵測領域 de facto 的開源實作，PyPI 月下載量超過數百萬，社群教材與商業導入皆極為普遍。
2. **可觀察性**：作為純本地推論工具，理論上應具備「斷網可用」的特性；此特性可作為 Gate-4 的明確驗證點。
3. **議題完整性**：Ultralytics 同時涉及代表性的開源風險議題 — 強 copyleft 授權（AGPL-3.0）、商業公司維護、內建遙測、模型權重 CDN 下載 — 適合作為第一個學習案例。

本案例的目標不是「證明 YOLO 安全」，而是「實際走過一次四關卡並記錄判斷過程」。每個被接受的風險均於對應段落明示。

## 第二節：Gate-1 — 信譽

**完整原始紀錄**：[`doc/R1_reputation.md`](doc/R1_reputation.md)

於 GitHub 頁面（<https://github.com/ultralytics/ultralytics>）做兩分鐘的快速判斷，得到的關鍵數字為：

- Stars：**56,641**（業界 de facto 標準）
- 最後更新：每日皆有 commit（`pushed_at = 2026-04-30`）
- Contributors：約 356 人，核心由 Ultralytics 員工主導
- 組織：Ultralytics Inc.，英國商業公司，YOLOv5 / v8 / v11 原作者
- License：**AGPL-3.0**

技術面通過。但 license 為 AGPL-3.0 — 這是一個強 copyleft 授權，其影響不僅限於「程式碼是否開源」，還包含：

- 自用 / 學習：無問題。
- 嵌入學員作品集：屬使用者自負範圍，但若公開散布作品則需以 AGPL 散布。
- 部署為 SaaS：使用者透過網路使用 SaaS 即觸發 AGPL 的「網路散布」條款，需開放整個服務的原始碼，或購買 Ultralytics 的商業授權。

**Gate-1 判定**：CONDITIONAL PASS ⚠️。技術面通過；AGPL 條款於 [`進階.md`](進階.md) §AGPL 商用評估中進一步處理。

## 第三節：Gate-2 — 依賴

**完整原始紀錄**：[`doc/R2_dependency.md`](doc/R2_dependency.md)

於尚未本地 clone 的前提下，透過 GitHub raw 內容檢視關鍵檔案。本階段檢查三項：

### 3.1 依賴清單

讀取 `pyproject.toml`，核心依賴清單如下：

```
numpy, matplotlib, opencv-python, pillow, pyyaml,
requests, scipy, torch, torchvision, psutil,
polars, ultralytics-thop
```

全部為主流 Python 套件，無 typosquatting 嫌疑（每一個皆能於 PyPI 第一頁找到）。可選依賴分為 6 組（`dev` / `export` / `solutions` / `logging` / `extra` / `typing`），不主動安裝即不引入。

### 3.2 安裝腳本

`pyproject.toml` 中無 `postinstall`、無自定義 build hook。`pip install ultralytics` **不會在使用者環境執行任意外部腳本**。此為通過的關鍵條件。

### 3.3 對外連線盤點

於 `ultralytics/utils/__init__.py` 與 `ultralytics/hub/utils.py` 中盤點到的硬寫 URL 共五類，整理如下：

| URL | 用途 | 觸發條件 |
|-----|------|---------|
| `github.com/ultralytics/assets/...` | 模型權重下載 | 第一次使用某模型時 |
| `api.ultralytics.com` | Hub API（雲端訓練） | 僅登入 Hub 後 |
| `hub.ultralytics.com` | Hub 網站 | 僅登入 Hub 後 |
| Sentry DSN（`o4504521589325824.ingest.us.sentry.io`） | crash 回報 | 需另裝 `sentry-sdk` 且 `sync=True` |
| Google Analytics | 使用統計 | `sync=True`（預設） |

**重要觀察**：純本地推論僅需要第一條 URL。其餘四項在套件預設不裝 `sentry-sdk` 與使用者主動執行 `sync=False` 後，皆不會觸發。Sentry DSN 公開於原始碼中是 Sentry SDK 的設計（client-side key），非異常。

**Gate-2 判定**：PASS ✅。可進入安裝階段，但安裝後第一個動作必須為關閉遙測（詳見下節）。

## 第四節：Gate-3 — 安裝與執行時驗證

**完整原始紀錄**：[`doc/R3_runtime.md`](doc/R3_runtime.md)

本階段於專屬 venv 中執行，禁止系統 Python 直接安裝。執行步驟如下：

### 4.1 環境隔離與安裝

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip ultralytics pip-audit
```

實際安裝包數：57 個套件，下載量約 2.3 GB（torch 與 opencv-python 為大宗），耗時約 3 分鐘。安裝過程未觸發任何外部腳本（與 Gate-2 觀察一致）。

### 4.2 立即關閉遙測

```powershell
.\.venv\Scripts\yolo.exe settings sync=False
```

設定持久化於 `%APPDATA%\Ultralytics\settings.json`：

```json
{
  "sync": false,
  ...
}
```

> 📸 截圖待補：`yolo settings sync=False` 執行後輸出。

此動作關閉 Google Analytics。Sentry 因 `sentry-sdk` 未安裝本即不啟用，無需額外處理。

### 4.3 CVE 掃描

```powershell
.\.venv\Scripts\pip-audit.exe
```

結果：**No known vulnerabilities found** ✅。涵蓋全部 57 個已安裝套件，包含 torch、opencv-python、requests 等歷史上有 CVE 紀錄的大型依賴。

> 📸 截圖待補：`pip-audit` 輸出。

### 4.4 模型權重下載與 hash 記錄

```powershell
Invoke-WebRequest "https://github.com/ultralytics/assets/releases/download/v8.3.0/yolo11n.pt" `
    -OutFile .\weights\yolo11n.pt
Get-FileHash -Algorithm SHA256 .\weights\yolo11n.pt
```

結果：

| 檔案 | 大小 | SHA256 |
|------|------|--------|
| `yolo11n.pt` | 5,613,764 bytes | `0EBBC80D4A7680D14987A577CD21342B65ECFD94632BD9A8DA63AE6417644EE1` |

此處出現本案例最重要的非預期發現，記錄於下一節。

## 第五節：意外發現 — `.pt` 權重缺乏官方 SHA256

於 Gate-1 與 Gate-2 階段，根據 GitHub 主頁與部分 release 文件的觀察，曾推論「Ultralytics 為每個模型權重檔提供 SHA256」。實際於 Gate-3 比對時，透過 GitHub Releases API 查詢：

```bash
curl -s "https://api.github.com/repos/ultralytics/assets/releases/tags/v8.3.0" \
    | python -c "import json,sys; d=json.load(sys.stdin); [print(a['name'], a.get('digest','(no digest)')) for a in d['assets'] if 'yolo11n' in a['name']]"
```

結果（節錄）：

```
yolo11n.onnx     sha256:634279b40c07c6391472c51ad45b81ebc48706a9a1fe72dd3396322acd0c053b
yolo11n.pt       None
yolo11n-cls.onnx sha256:2d2f6414daeced6c518bb13f92b17fc1dbcaad60278b8f3ed5edd341c35abcd4
yolo11n-cls.pt   None
yolo11n-pose.pt  None
...
```

**結論**：Ultralytics 在 GitHub release metadata 中**僅為 `.onnx` 格式提供 SHA256，`.pt` 格式的 `digest` 欄位為 `null`**。原先在 R1 / R2 報告中「每個 .pt 都有 SHA256」的推論為誤判。

此發現的實務影響：

1. 對 `.pt` 權重，無法以「比對官方 hash」進行供應鏈驗證。改採 **trust-on-first-use** 策略：將首次下載計算出的 hash 釘入專案文件（即 §4.4 表格中的 hash），未來任何不一致均視為警告。
2. 對於有嚴格供應鏈驗證需求的場景（生產環境、商用部署），建議使用 ONNX 格式（透過 `model.export(format='onnx')` 轉換或直接從 release 下載）。

此發現亦展示流程的價值：**Gate 與 Gate 之間不是單向的「通過即離開」，而是後續關卡可以反向修正前一關的觀察**。R1 與 R2 報告中均已加上對應的修正註記。

## 第六節：終點 — webcam.py 跑通

完整代碼：[`webcam.py`](webcam.py)

於前述環境配置完成後，實際執行：

```powershell
.\.venv\Scripts\python.exe webcam.py
```

開啟預設 webcam，於畫面上即時繪製偵測框、類別、信心值與 FPS 計數。按 `q` 結束。

> 📸 截圖待補：webcam.py 運行畫面（含偵測框、FPS 數字）。
>
> 📹 demo 影片待補：30 秒短片展示即時偵測。

於本案例的執行環境（CPU、無獨立顯卡），實測效能：

- 單次推論：約 60–80 ms / 幀
- 等效 FPS：12–16
- 偵測準確度：對日常物件（人、椅子、瓶子、筆電）穩定；對細小或反光物件偶有誤判

此效能於教學與輕量應用場景充分可用。如需更高 FPS 可換較小模型或啟用 GPU 推論。

## 第七節：四關卡執行回顧

完成本案例後，可歸納幾項實務體會：

1. **Gate-2 是時間最長但價值最高的關卡**。原始碼層級的依賴與連線盤點，能在不執行任何代碼的前提下排除大部分風險。
2. **遙測關閉應為安裝後第一個動作**，而非「之後再說」。多數套件的遙測在使用者執行第一個指令時即觸發。
3. **不要假設文件正確**。本案例中 R1 / R2 對 hash 的推論被 R3 證偽 — 流程的價值不在於避免犯錯，而在於提供修正錯誤的結構。
4. **流程的時間成本約 90 分鐘**，其中半數為等待（PyTorch 下載 + venv 安裝）。實際思考與決策的時間遠少於此。

---

## 動手清單

於閱讀完本文後，建議讀者於本機重複執行以下步驟：

- [ ] clone 本 repo 至本機
- [ ] 建立 venv 並安裝套件（[`案例.md` §4.1](#41-環境隔離與安裝)）
- [ ] 關閉遙測（§4.2）
- [ ] 跑 `pip-audit` 掃描（§4.3）
- [ ] 下載 yolo11n.pt 並比對本文 §4.4 的 hash
- [ ] 執行 [`webcam.py`](webcam.py) 並觀察結果
- [ ] 於 [`doc/R3_runtime.md`](doc/R3_runtime.md) 對照原始紀錄

完成上述清單後，建議閱讀 [`進階.md`](進階.md) 探索 YOLOE、訓練、ONNX、AGPL 商用評估等延伸主題。
