# 進階主題

本文延伸 [`案例.md`](案例.md) 的基本實作，介紹四個進階方向。每節僅作概念引導與起步指令，不展開為完整教學；讀者可依興趣選擇深入。

---

## 第一節：YOLOE — 用文字提示偵測自訂物件

### 概念

傳統 YOLO 偵測 80 種預定義類別（COCO 資料集），增加新類別需重新訓練。**YOLOE**（YOLO with text prompts）為 Ultralytics 在 2024 年起推出的開放詞彙（open-vocabulary）變種，允許使用者以**文字描述**指定要偵測的物件，無需重新訓練。

### 起步

```python
from ultralytics import YOLOE

model = YOLOE("yoloe-11s-seg.pt")
model.set_classes(["coffee mug", "smartphone", "houseplant"])
results = model.predict(source=0, show=True)
```

`set_classes()` 接受任意英文短語。模型會以文字編碼器將這些短語對映到視覺特徵空間，而後執行偵測。

### 適用場景

- 偵測 COCO 80 類以外的物件，但資料量不足以訓練。
- 需要快速原型驗證（demo / 概念驗證）。
- 偵測類別會頻繁變動的應用。

### 限制

- 對細微差異（同類別內的次型）區分能力較弱。
- 對極專業領域（醫學影像、工業缺陷）效果不及領域訓練模型。
- 模型大小較傳統 YOLO11 大（推論較慢）。

### 對應的安全考量

YOLOE 仍屬 Ultralytics 套件範疇，[`開源安全檢查流程.md`](開源安全檢查流程.md) 的四關卡判斷一併適用。模型權重亦為 `.pt` 格式，hash 處理同 [`案例.md` §5](案例.md#第五節意外發現-pt-權重缺乏官方-sha256)。

---

## 第二節：訓練自訂偵測器

### 概念

當 YOLOE 的開放詞彙能力不足以處理特定領域時，需以自訂資料微調 YOLO 模型。最低需求約 50–200 張標註影像即可獲得堪用模型。

### 流程概覽

1. **資料蒐集**：拍攝目標物件，建議覆蓋不同角度、光線、背景。
2. **標註**：使用 [LabelImg](https://github.com/HumanSignal/labelImg)、[Roboflow](https://roboflow.com)、[CVAT](https://www.cvat.ai/) 等工具產出 YOLO 格式的 `.txt` 標註檔。
3. **資料切分**：train / val / test，常見比例 70 / 20 / 10。
4. **訓練**：

   ```python
   from ultralytics import YOLO
   model = YOLO("yolo11n.pt")
   model.train(
       data="path/to/data.yaml",
       epochs=100,
       imgsz=640,
       device=0,  # GPU index；CPU 訓練可行但極慢
   )
   ```

5. **驗證**：以 `val` 子集評估 mAP（mean Average Precision）。
6. **匯出**：轉為部署格式（ONNX / TFLite / CoreML）。

### 硬體建議

- **CPU 訓練**：可行但耗時。100 epochs / 200 張影像於現代 CPU 約需 4–8 小時。
- **GPU 訓練**：RTX 3060 級別已可勝任多數小型專案，同樣設定約 30–60 分鐘。
- **雲端**：Colab Free 提供 T4 GPU，適合一次性實驗。

### 對應的安全考量

訓練本身不引入新的供應鏈風險，但若使用 Roboflow / Colab 等雲端服務，**訓練資料會離開本機**。對含敏感資訊（人臉、私有產品設計、醫療影像）的資料，應於本地完成標註與訓練。

---

## 第三節：PyTorch → ONNX — 跨格式部署

### 概念

`.pt` 為 PyTorch 原生格式，部署需安裝 PyTorch（~2 GB）。**ONNX**（Open Neural Network Exchange）為跨框架的中間格式，可於以下執行環境運行：

- **桌面 / 伺服器**：onnxruntime（Python / C++ / Rust）
- **瀏覽器**：[onnxruntime-web](https://onnxruntime.ai/docs/tutorials/web/)（純 JS，無需後端）
- **行動裝置**：onnxruntime-mobile（iOS / Android）
- **嵌入式**：透過進一步轉換為 TensorRT / OpenVINO

### 轉換指令

```python
from ultralytics import YOLO
model = YOLO("yolo11n.pt")
model.export(format="onnx")  # 產出 yolo11n.onnx
```

### 為什麼 ONNX 對本教材重要

如 [`案例.md` §5](案例.md#第五節意外發現-pt-權重缺乏官方-sha256) 所述，Ultralytics 為 `.onnx` 提供官方 SHA256，但 `.pt` 沒有。對於有嚴格供應鏈驗證需求的場景：

1. 直接從官方 release 下載 `.onnx` 並比對 hash，**而非**自行從 `.pt` 轉換。
2. 部署環境僅安裝 onnxruntime（~50 MB），無需 PyTorch。

此外，ONNX 模型於瀏覽器端運行時，使用者的影像資料不離開瀏覽器，對隱私敏感應用（如人像偵測）有實質意義。

### 限制

- 部分 PyTorch 動態算子難以轉換，需手動處理。
- ONNX 模型的後處理（NMS、座標還原）需自行實作，不像 `.pt` 透過 ultralytics 套件自動完成。

---

## 第四節：AGPL-3.0 商用評估

[`案例.md` §2](案例.md#第二節gate-1--信譽) 已指出 Ultralytics 採 AGPL-3.0。本節提供商用情境的決策框架。

### AGPL-3.0 的關鍵條款

相對於常見的 MIT / Apache-2.0 / GPL-3.0：

- **網路散布條款**：使用者透過網路使用 AGPL 軟體（如 SaaS）即觸發散布條件，必須將原始碼公開予該使用者。此為 AGPL 與 GPL 最大差異。
- **衍生作品定義**：直接 `import` 並調用 API，多數法律意見認為構成衍生作品，整個應用須以 AGPL 散布。

### 商用情境決策表

| 情境 | AGPL 是否觸發 | 處理方式 |
|------|-------------|---------|
| 個人 / 學術自用 | 不觸發散布 | 無需處理 |
| 內部企業工具，僅員工使用 | 不觸發網路散布 | 無需處理（保險：保留 AGPL 通知） |
| 對外網站使用 YOLO 處理使用者上傳影像 | **觸發** | 三選一：開源整個服務 / 改用 ONNX 於瀏覽器端推論 / 購買 Ultralytics 商業授權 |
| 嵌入桌面軟體並販售 | **觸發**（散布二進位即觸發） | 二選一：軟體採 AGPL 散布 / 購買商業授權 |
| 嵌入嵌入式裝置（攝影機、機器人） | **觸發** | 同上 |

### Ultralytics 商業授權

Ultralytics 提供企業授權方案（[ultralytics.com/license](https://www.ultralytics.com/license)），具體價格需洽詢。一般而言，授權費用相對於完整自行開源後端（含維運、合規）的成本，於商業情境下多數能接受。

### 替代方案

若預算不允許商業授權且不願全面開源：

1. **YOLOX / YOLOv7**：採 Apache-2.0，無 AGPL 限制，但維護活躍度與生態系不及 Ultralytics。
2. **rtmdet**（OpenMMLab）：Apache-2.0，準確度與速度與 YOLO11 相當。
3. **YOLO-NAS**（Deci AI）：商業友好授權，但 Deci 已於 2024 被收購，後續維護未明。

### 建議

於專案啟動初期即明確商業意圖。若有任何對外服務或產品散布計畫，**第一個 commit 之前**就應做出 license 決策（採 AGPL / 採商業授權 / 改用其他模型）。事後切換的成本遠高於預先選擇。

---

## 第五節：進一步資源

### Ultralytics 官方

- 文件站：<https://docs.ultralytics.com>
- GitHub：<https://github.com/ultralytics/ultralytics>
- 商業授權：<https://www.ultralytics.com/license>
- 隱私政策：<https://docs.ultralytics.com/help/privacy/>

### 開源安全工具

- `pip-audit`：Python 套件 CVE 掃描，PyPA 官方背書
- `safety`：另一個 Python CVE 工具，社群維護
- `OpenSSF Scorecard`：自動評估 GitHub 專案的安全實踐分數

### 物件偵測進階學習

- *Object Detection in 20 Years: A Survey* (2019)：物件偵測領域回顧
- COCO 資料集：<https://cocodataset.org>，YOLO 預設訓練資料
- Papers with Code 物件偵測排行榜：<https://paperswithcode.com/task/object-detection>

### 相關開源安全事件案例

下列事件可作為「為什麼要做四關卡」的具體說明材料：

- **2018 event-stream（npm）**：知名套件被惡意接管，竊取加密貨幣錢包。
- **2022 PyTorch nightly（PyPI）**：依賴混淆攻擊，惡意套件搶先發布同名模組。
- **2023 colorama / 各類 typosquatting**：拼字相近套件竊取資料。
- **2024 XZ Utils（Linux）**：歷時三年的供應鏈滲透，幾乎影響全球 Linux 伺服器 SSH。

這些事件均印證 [`開源安全檢查流程.md` §1](開源安全檢查流程.md#第一節流程的必要性) 的主張：開源生態的「方便假象」需被主動拆解。
